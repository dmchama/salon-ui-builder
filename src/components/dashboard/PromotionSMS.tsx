const PromotionSMS = () => (
  <div className="text-center py-16 px-4 text-muted-foreground text-sm max-w-lg mx-auto tracking-wide">
    SMS campaigns and promotional blasts are not available in the backend yet. Guest contact details on each booking can be
    used manually if you run external tools.
  </div>
);

export default PromotionSMS;
